export interface Student {
    id? : string,
    full_name ?: string,
    surname? : string,
    phone_number? : string,
    user_id? : string,
    birth_date ?: Date,
    created_at ?: Date,
    updated_at ?: Date,
    age ?: string,
    level ?: string
}
